import { ThemePalette } from '@angular/material/core';

export interface ThemeConfig {
  toolbarColor?: ThemePalette;
  navColor?: ThemePalette;
  navBackgroundColor?: ThemePalette;
  icon: string;
}
